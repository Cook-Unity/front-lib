import React from 'react'

import CuiMealBadge from './CuiMealBadge'

export default {
  title: 'Cui / Modules / CuiMealCard / Components / CuiMealBadge',
  component: CuiMealBadge
}

const Template = args => <CuiMealBadge {...args}>New</CuiMealBadge>

export const Default = Template.bind({})
