import React from 'react'

import CuiSpinner from './CuiSpinner'

export default {
  title: 'Cui / Components / CuiSpinner',
  component: CuiSpinner
}

const Template = args => (
  <div>
    <CuiSpinner width="24px" />
  </div>
)

export const Default = Template.bind({})
