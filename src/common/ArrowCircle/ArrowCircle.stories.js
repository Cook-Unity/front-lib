import React from 'react'

import ArrowCircle from './index'

export default {
  title: 'Atoms/ArrowCircle',
  component: ArrowCircle
}

const Template = args => <ArrowCircle {...args} />

export const Default = Template.bind({})
